public class LibraryLoginSystem {
    public static void main(String[] args) {
        // Daftar pengguna
        String adminUsername = "admin";
        String adminPassword = "admin123";
        String mahasiswaUsername = "mahasiswa";
        String mahasiswaPassword = "mahasiswa123";

        // Input username dan password
        Scanner scanner = new Scanner(System.in);
        System.out.print("Username: ");
        String username = scanner.nextLine();
        System.out.print("Password: ");
        String password = scanner.nextLine();

        // Verifikasi pengguna
        if (username.equals(adminUsername) && password.equals(adminPassword)) {
            System.out.println("Login berhasil sebagai admin.");
            // Tambahkan logika untuk aksi admin di sini
        } else if (username.equals(mahasiswaUsername) && password.equals(mahasiswaPassword)) {
            System.out.println("Login berhasil sebagai mahasiswa.");
            // Tambahkan logika untuk aksi mahasiswa di sini
        } else {
            System.out.println("Username atau password salah.");
        }

        scanner.close();
    }
}public class LibraryLoginSystem {
    public static void main(String[] args) {
        // Daftar pengguna
        String adminUsername = "admin";
        String adminPassword = "admin123";
        String mahasiswaUsername = "mahasiswa";
        String mahasiswaPassword = "mahasiswa123";
    }
}

        // Input username dan password
        Scanner scanner = new Scanner(System.in);
        System.out.print("Username: ");
        String username = scanner.nextLine();
        System.out.print("Password: ");
        String password = scanner.nextLine();

        // Verifikasi pengguna
        if (username.equals(adminUsername) && password.equals(adminPassword)) {
            System.out.println("Login berhasil sebagai admin.");
            // Tambahkan logika untuk aksi admin di sini
        } else if (username.equals(mahasiswaUsername) && password.equals(mahasiswaPassword)) {
            System.out.println("Login berhasil sebagai mahasiswa.");
            // Tambahkan logika untuk aksi mahasiswa di sini
        } else {
            System.out.println("Username atau password salah.");
        }

        scanner.close();
    }
}public class LibraryLoginSystem {
    public static void main(String[] args) {
        // Daftar pengguna
        String adminUsername = "admin";
        String adminPassword = "admin123";
        String mahasiswaUsername = "mahasiswa";
        String mahasiswaPassword = "mahasiswa123";

        // Input username dan password
        Scanner scanner = new Scanner(System.in);
        System.out.print("Username: ");
        String username = scanner.nextLine();
        System.out.print("Password: ");
        String password = scanner.nextLine();

        // Verifikasi pengguna
        if (username.equals(adminUsername) && password.equals(adminPassword)) {
            System.out.println("Login berhasil sebagai admin.");
            // Tambahkan logika untuk aksi admin di sini
        } else if (username.equals(mahasiswaUsername) && password.equals(mahasiswaPassword)) {
            System.out.println("Login berhasil sebagai mahasiswa.");
            // Tambahkan logika untuk aksi mahasiswa di sini
        } else {
            System.out.println("Username atau password salah.");
        }

        scanner.close();
    }
}